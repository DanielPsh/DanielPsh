#include <iostream>
using namespace std;

/*
int minMaxAvg(int num[], int size)
{
    int min = 1000;
    int max = 0;
    double avg = 0;
    double sum = 0;

    for(int i = 0; i < size; i++)
    {
        if(num[i] < min)
        {
            min = num[i];
        }else if(num[i] > max)
        {
            max = num[i];
        }
        sum = sum + num[i];
    }

    avg = sum / size;

    cout << "The min: " << min << endl;
    cout << "The max: " << max << endl;
    cout << "The avg: " << avg << endl;

    return 0;

}
*/

void minMaxAvg(int num[], int size, int& min, int& max, double& avg)
{
    
}

int main()
{
    /*
    int A[5] = {3,6,7,2,9};
    //minMaxAvg(A, 5);
    int result1,result2;
    double result3;
    minMaxAvg(A, 5, result1, result2, result3);
    */

    //double v;
    //cout << sizeof(v) << endl; //number of bytes to store the value

    /*
    int abc = 888;
    cout << &abc << endl;

    int abc1 = 888;
    cout << abc1 << endl;
    */

    /*
    int num1 = 10;
    int *a = &num1;
    cout << sizeof(num1) << endl;
    cout << sizeof(a) << endl;
    
    char char1 = 'A';
    char *b = &char1;
    cout << sizeof(char1) << endl;
    cout << sizeof(b) << endl;

    double num2 = 3.5;
    double *c = &num2;
    cout << sizeof(num2) << endl;
    cout << sizeof(c) << endl;
    */

    
}