[package]
name = "task-queue"
version = "0.1.0"
edition = "2024"

[dependencies]
blaze-core = { package = "core", path = "../core" }

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "queue_benchmark"
harness = false
