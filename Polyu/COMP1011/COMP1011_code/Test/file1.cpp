#include <iostream>
using namespace std;
int main() {
	int counter = 1; // start the counter
	while (counter <= 5) { // check the condition
		cout << counter << endl; // print the value
		counter++; // update the counter
	}
	return 0;
}