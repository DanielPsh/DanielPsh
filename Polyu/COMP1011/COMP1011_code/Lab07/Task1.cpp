#include <iostream>
using namespace std;

int main()
{
    // Function Prototype
    char propercase(char z);
    void swapit(int &a, double &b);
    double cylinder_height(int h, double r);
    // Variables in main()
    int f;
    double g;
    double x;
    char greetings;

    //propercase('a') = greetings; -> assigning a value to a function cell is invalid

    //double_swap_it(f,g); -> function name is incorrect

    //f = cylinder_height(g,x); -> Argument type mismatch

}